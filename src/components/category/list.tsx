import useBook from "@/hooks/useBook";
import BookLayout from "../book/bookItem";

const CategoryList = () => {
  const { setBook, books } = useBook();
  return <div></div>;
};

export default CategoryList;
